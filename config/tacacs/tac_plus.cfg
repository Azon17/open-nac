id = spawnd {
    listen = { port = 49 }
    spawn = {
        instances min = 1
        instances max = 10
    }
    background = no
}
id = tac_plus {
    debug = AUTHOR NET
    key = testing123
    user = admin {
        password = clear admin123
        service = shell {
            default command = permit
            set priv-lvl = 15
        }
    }
}
