modules:
  https_cert:
    prober: http
    timeout: 10s
    http:
      method: GET
      fail_if_ssl: false
      tls_config:
        insecure_skip_verify: true
      valid_status_codes: []  # Accept any status, we just want the cert

  tcp_connect:
    prober: tcp
    timeout: 5s
