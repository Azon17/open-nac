redis {
    server = "redis-1"
    port = 6379
    password = "${env.REDIS_PASSWORD:-changeme_redis_pwd}"
    database = 0
    pool {
        start = 5
        min = 3
        max = 20
        spare = 5
    }
}
