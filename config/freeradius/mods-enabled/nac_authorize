rest nac_authorize {
    connect_uri = "http://policy-engine:8000"
    authorize {
        uri = "${..connect_uri}/api/v2/authorize"
        method = "post"
        body = "json"
        force_to = "json"
        timeout = 5
    }
    pool {
        start = 0
        min = 0
        max = 20
        spare = 3
        uses = 0
        retry_delay = 10
        lifetime = 0
        idle_timeout = 60
    }
}
